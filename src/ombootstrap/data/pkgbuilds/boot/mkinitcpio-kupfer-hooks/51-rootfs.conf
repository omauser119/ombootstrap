# Find the real Kupfer partition and let the normal initramfs mount it.
# Rootfs checks/resizing are intentionally omitted: an Android boot must not
# stall on e2fsck before systemd gets control.
HOOKS+=(rootfsdetect filesystems keyboard)
