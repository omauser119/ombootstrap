# Backlight device: leave empty for automatic discovery through brightnessctl.
# Set this to the actual device from `brightnessctl -l`, not an example from
# another phone (ae94000.dsi.0 is not assumed to exist on a6plte).
BACKLIGHT_DEVICE=
